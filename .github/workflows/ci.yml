name: CI
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.3
      - uses: actions/setup-go@v2
      - run: docker-compose up -d
      - run: env | sort
      - run: make lint
      - run: make test
